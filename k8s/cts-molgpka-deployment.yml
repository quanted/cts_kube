apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: cts-molgpka
  name: cts-molgpka
  namespace: qed-kube-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cts-molgpka
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: cts-molgpka
    spec:
      containers:
      - name: cts-molgpka
        image: ghcr.io/quanted/cts-molgpka:dev-2ccc488a52059c851f146c44e77ba4d8ce8476a6
        imagePullPolicy: Always
        livenessProbe:
          exec:
            command:
              - ls
              - /
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 10
        readinessProbe:
          exec:
            command:
              - ls
              - /
          initialDelaySeconds: 10
          periodSeconds: 30
          timeoutSeconds: 10
        ports:
        - containerPort: 8080
          protocol: TCP
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
