apiVersion: v1
kind: Service
metadata:
  name: cts-mongodb
  namespace: qed-kube-dev
spec:
  ports:
  - port: 27017
    protocol: TCP
    targetPort: 27017
  selector:
    app: cts-mongodb
  type: ClusterIP
